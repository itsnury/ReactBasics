import {Component} from 'react';

class CardList extends Component {
    render() {
        // when you return a component make sure there's only one
        // top level component
        return (
            <div>Hello I'm the Card List</div>
        )
    }
}

export default CardList